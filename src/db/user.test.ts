describe('Testing user', () => {

  it('Should failed when creating a user without details', () => {
    console.log('a');
  });

  it('Should failed when passing wrong email', () => {
    console.log('a');
  });

  it('Should encrypt the password', () => {
    console.log('a');
  });

  it('Should auto fill the created at when creating a user', () => {
    console.log('a');
  });

  it('Should changed the updated at when updating the user', () => {
    console.log('a');
  });
});
