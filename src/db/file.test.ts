describe('File testing', () => {

  it('Should save a file with valid values: nullable storage ID and valid status', () => {
    expect(1).toBeFalsy();
  });

  it('Should fail when setting invalid status', () => {
    expect(1).toBeFalsy();
  });

  it('Updating a file status based on the storage ID', () => {
    expect(1).toBeFalsy();
  });
});
